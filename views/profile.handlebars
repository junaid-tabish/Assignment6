{{> header2}}
  <div class="container">
    <div class="row">
              <div class="card-body p-4">
                <h3>{{data.name}}'s Profile</h3>
                <hr class="mt-0 mb-4">
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h5><u>Email</u></h5>
                    <p class="text-muted">{{data.email}}</p>
                  </div>
                  <div class="col-6 mb-3">
                    <h5><u>Phone</u></h5>
                    <p class="text-muted">{{data.mobile}}</p>
                  </div>
                </div>
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h5><u>Address</u></h5>
                    <p class="text-muted">{{data.address}}</p>
                  </div>
                </div>
                <a href="/updateuser/{{data._id}}" class="btn btn-dark" >Edit Profile</a>
                <a href="/deleteuser/{{data._id}}" class="btn btn-dark" >delete Account</a>
                <a href="/myorders/{{data.email}}" class="btn btn-dark" >My Orders</a>
       </div>
    </div>
  </div>