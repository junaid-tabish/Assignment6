{{> header2}}
<section class="container">
        <div class="container py-3">
            <h3>Menu</h3>
            <div class="row" style="justify-content: space-between;">
                {{#each data}}
                <div class="card m-2" style="width: 22rem;">
                    <img src="{{image}}" class="card-img-top p-2" alt="..." height="250px">
                    <div class="card-body">
                        <h5 class="card-title text-center">{{name}}</h5>
                        <div class="d-flex">
                            <div class="mx-4">
                                <a href="/cart/{{_id}}" class="btn btn-dark">Add to cart</a><br></br>
                                <a href="/view/{{_id}}" class="btn btn-dark">View</a>
                            </div>
                            <div class="mx-4">
                                <p>₹: {{price}}</p>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}

            </div>
        </div>
</section>
