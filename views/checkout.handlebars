<body>
{{> header2}}
<div class="container">
<h3 class="mt-3">Checkout</h3>
<form name="checkout" action="/chekouthandle" method="post">
  <label class="form-label" for="typeText">Credit Card</label>
  <input
    type="number"
    id="typeText"
    class="form-control form-control-lg"
    size="17"
    minlength="16"
    maxlength="16"
    pattern="[0-9]{16}"
    title="Credit card number is required and must be 16 digits"
    required
  />

<p>Order Total: <strong>₹ {{amount}}</strong> </p>

<button type="submit" class="btn btn-dark">Checkout</button>
</form>
</div>
